{{ form_start(form) }}

    <div class="input-group input-group-outline mb-3">
    {{ form_widget(form.contenu, {'attr': {'class': 'form-control', 'placeholder': 'Enter your name'}}) }}
</div>

<div class="input-group input-group-outline mb-3">
    {{ form_widget(form.reclamation, {'attr': {'class': 'form-control', 'placeholder': 'Enter your comment '}}) }}
</div>

    <div class="text-center">
 <button  type="submit" class="btn btn-lg bg-gradient-primary btn-lg w-100 mt-4 mb-0">{{ button_label|default('Save') }}</button>
     {{ form_widget(form) }} 
</div>
{{ form_end(form) }}
